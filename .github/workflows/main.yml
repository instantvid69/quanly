name: quanly

on:
  push:
    branches: 
      - main
  workflow_dispatch:

env:
  BUILD_NUMBER: 1
  BUILD_VERSION: 1.0.0
  PYTHON_VERSION: 3.12.2
  PYTHONUTF8: "1"

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    # ÉP MÁY ẢO DÙNG ĐÚNG BẢN FLET 0.28.3 ĐỂ BUILD
    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flet==0.28.3
        pip install -r requirements.txt

    # CHẠY LỆNH BUILD (Flet 0.28.3 sẽ tự động cấu hình Flutter chuẩn của nó)
    - name: Flet Build Windows
      run: |
        flet build windows --name quanly --verbose --build-number=${{ env.BUILD_NUMBER }} --build-version=${{ env.BUILD_VERSION }}

    - name: Zip Windows Build
      shell: python
      run: |
        import shutil
        import os
        if os.path.exists('build/windows'):
            shutil.make_archive('quanly-windows', 'zip', 'build/windows')
        else:
            print("Error: Thư mục build/windows không tồn tại!")
            exit(1)

    - name: Upload Windows Artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-zip
        path: quanly-windows.zip
        if-no-files-found: error
        overwrite: true

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download Windows Artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-build-zip

      - name: Release
        uses: jasondavis303/tag-and-release-and-upload@v1.2.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          version: v${{ env.BUILD_VERSION }}
          assets: '[ "quanly-windows.zip" ]'
          overwrite: false
